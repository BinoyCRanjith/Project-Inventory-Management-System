@model List<OneTeamAptitudeMVC.Models.MarkData>
<meta name="viewport" content="width=device-width, initial-scale=1">
<style>
    body {
        margin: 0;
        font-family: Arial, Helvetica, sans-serif;
    }

    .topnav {
        overflow: hidden;
        background-color: #333;
    }

        .topnav a {
            float: left;
            color: #f2f2f2;
            text-align: center;
            padding: 14px 16px;
            text-decoration: none;
            font-size: 17px;
        }

            .topnav a:hover {
                background-color: #ddd;
                color: black;
            }

            .topnav a.active {
                background-color: #04AA6D;
                color: white;
            }
</style>

<body>

    <div class="topnav">
        <a class="active" href="http://localhost:50492/">Home</a>
        <a href="InsertMark","Marks">Create New Mark</a>
    </div>


</body>

<div class="row">
    <table ID="editabledatatable" class="table table-striped table-hover table-bordered dt-responsive display" style=" margin-left: 36px !important;">
        <thead>
            <tr>
                <th>Name</th>
                <th>UserID</th>
                <th>SubjectID</th>
                <th>Subjectname</th>
                <th>Mark</th>




                <th>Action</th>

            </tr>
        </thead>


        @if (Model != null)
        {
            foreach (var item in Model)
            {
                <tr>
                    <td>
                        @item.Name
                    </td>
                    <td>
                        @item.UserID
                    </td>
                    <td>
                        @item.SubjectID
                    </td>
                    <td>
                        @item.Subjectname
                    </td>
                    <td>
                        @item.Mark
                    </td>


                    <td>
                        <div class="btn-group">
                            <a class="btn btn-azure dropdown-toggle" data-toggle="dropdown">
                                Actions&nbsp;&nbsp; <i class="fa fa-angle-down"></i>
                            </a>
                            <ul class="dropdown-menu dropdown-azure">

                                <li>
                                    @Html.ActionLink("Edit", "InsertMark", "Marks", new { @ID = item.ID }, new { @name = "Edit" })

                                </li>
                                <li>
                                    @Html.ActionLink("Disable", "Delete", "Marks", new { @ID = item.ID }, new { onclick = "return confirm('Are you sure?');" })

                                </li>
                            </ul>
                        </div>


                    </td>
                </tr>

            }

        }

    </table>
</div>

@section scripts{
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-validate/1.19.2/jquery.validate.min.js"></script>
    <script>
        $(document).ready(function () {
            var message = "Message @TempData["Message"]";
            if (message.length > 8) {
                alert(message);
            }
        });
    </script>
}
